def above_freezing(celsius: float) -> bool:
    """ return true iff temp celsius degrees is above freezing.
    >>> above_freezing(5.2)
    True
    >>> above_freezing(-2)
    False
    """

    return celsius > 0
